{% extends 'base.html.twig' %}

{% block body %}
    <h1 class="page-header">Commune creation</h1>
    <a class="btn btn-primary" href="{{ path('commune_index') }}"><em class="glyphicon glyphicon-list"></em></a>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input class="btn btn-success" style="margin-top: 20px" type="submit" value="Create" />
    {{ form_end(form) }}
{% endblock %}
